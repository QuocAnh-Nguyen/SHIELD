torchvision==0.15.2
transformers==4.31.0
torch==2.0.1
tokenizers>=0.12.1,<0.14
sentencepiece==0.1.99
shortuuid
accelerate==0.21.0
peft==0.4.0
# bitsandbytes removed: unused by SHIELD (fp16 only), and its 0.41.0 CUDA setup
# fails on servers whose CUDA runtime libs live inside the pip torch wheel
# (libcusparse.so.11 not on LD_LIBRARY_PATH), breaking the transformers import
# chain through accelerate. Install manually only if quantized loading is needed.
numpy<2
scikit-learn==1.2.2
gradio==3.35.2
gradio_client==0.2.9
requests
httpx==0.24.0
uvicorn
fastapi
einops==0.6.1
einops-exts==0.0.4
timm==0.6.13

# CHAIR evaluation dependencies
pattern @ git+https://github.com/clips/pattern.git
nltk
tqdm